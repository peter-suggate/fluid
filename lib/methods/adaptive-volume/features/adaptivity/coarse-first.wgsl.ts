/** Linked into the resident shader against its accepted-cell and world-directory ABI.
 *
 * Feature-preserving coarsening sizes material by deformation and
 * representability rather than by a page's normal spread or an approaching
 * page's speed, so the restriction-density sampler, the page normal probe and
 * the incoming-impact retention floor it fed all became unreachable. Only the
 * mode flag survives; adaptive-mass keeps its own copy of the retired helpers.
 */
export const coarseFirstWGSL = /* wgsl */ `fn coarseFirstEnabled()->bool{return p.coarseFirst.x!=0.0;}
`;
