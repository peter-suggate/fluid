export default function ScenePage() {
  // The persistent app shell owns the studio mount. This page owns when that
  // mount is visible, without replacing its canvas during route navigation.
  return null;
}
