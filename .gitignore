node_modules/
.next/
dist/
.wrangler/
.vinext/
coverage/
.claude/
.DS_Store
*.log
*.tsbuildinfo
.env*
!.env.example
.build/
native/.build/
tmp/
artifacts/algorithm-diagnostics/
artifacts/octree-regression/
# Profiler output. A single xctrace capture is ~1 GB of .trace bundle and
# exported tables, and its report.html/summary.json carry every retained frame,
# so even the reduced outputs are megabytes. Regenerate them, do not commit
# them: `npm run profile:mini-dam-xctrace[:occupancy]`.
artifacts/xctrace-*/
